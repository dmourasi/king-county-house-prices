# Quarto build artifacts — os .html finais já são self-contained (embed-resources: true),
# então o que fica aqui é só cache/figuras intermediárias, não necessário para reproduzir.
analysis/*_cache/
analysis/*_files/
/.quarto/

# R
.Rhistory
.Rdata
.Rproj.user/

# Zip original do desafio — redundante com data/ e README.md (mesmo conteúdo, já extraído)
Desafio Técnico - GenAI.zip

# OS
.DS_Store
Thumbs.db

# Configuração/sessão de ferramenta local de desenvolvimento — não é conteúdo do projeto
.claude/

# Random Forest em Python: comparação apenas, não usada por nenhum notebook
# downstream (o modelo de referência é modelo_xgb.joblib). Regenerada ao
# rodar python/02-1_modelagem.ipynb; não precisa ficar versionada (25MB).
python/models/modelo_rf.joblib
